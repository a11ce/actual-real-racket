#lang info

(define collection "gnome")

(define scribblings '(("actual-real-racket.scrbl")))

; this does not apply to the gnome
(define license '(Apache-2.0 OR MIT))

(define deps '("base"))
(define build-deps '("scribble-lib"))
